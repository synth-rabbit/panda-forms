/* @flow */

const regex = {
  date: /(.*?(\$date).*)$/,
  checkbox: /(.*?(\$checkbox).*)$/,
  radio: /(.*?(\$radiobtn).*)$/,
  radioGroup: /(.*?(\$radiogrp).*)$/,
  expiration: /(.*?(\$expiration).*)$/,
  email: /(.*?(\$email).*)$/,
  credit: /(.*?(\$credit).*)$/
};

export {regex};
